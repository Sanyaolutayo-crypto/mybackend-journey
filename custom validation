const users =[
    {
        email: "joohn@gmail.com", phone:"08101787478"
    }
];

//Validation unique email 
function
validateUniqueEmail(email){
    const exists = users.find(user => user.email=== email);

    if (exists) {
        return "Email already exists";
    }

    return "Valid email";
}

//Validation age restriction
function